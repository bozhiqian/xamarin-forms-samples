<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="WorkingWithPopups.Views.ActionSheetPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:WorkingWithPopups.ViewModels;assembly=WorkingWithPopups"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ActionSheetPageViewModel,IsDesignTimeCreatable=True}">
    <ContentPage.Content>
        <StackLayout Padding="0,20,0,0">
            <Label Text="Display ActionSheet" FontSize="Large" HorizontalOptions="Center" />
            <Button Text="ActionSheet Simple" Command="{Binding ActionSheetSimpleCommand}" />
            <Button Text="ActionSheet Cancel/Delete" Command="{Binding ActionSheetCancelDeleteCommand}" />
            <Label Text="{Binding Action}" Margin="20" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>